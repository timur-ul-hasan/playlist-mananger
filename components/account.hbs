<main>
    <ul class="people_list">
        {{#each users}}
        <li>
            <span>
                <a href="/playlist/{{this.id}}">{{this.name}}</a>
            </span>
        </li>
        {{/each}}
    </ul>
</main>
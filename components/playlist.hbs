<main>
    <ul class="people_list">
        {{#each songs}}
        <li>
          <audio controls>
          <source src="/{{this.url}}" type="audio/mp3">
        </audio>
        </li>
        {{/each}}
    </ul>
    <div class="register-wrapper">
    <form action="/add-song" enctype="multipart/form-data" method="POST">
        <fieldset>
            <input type="hidden" name="playlistId" value="{{playlistId}}">
            <div class="form-group">
                <label class="form-label" for="input-example-19">Song name</label>
                <input class="form-input" type="text" id="input-example-19" placeholder="Name" name="name">
            </div>
            <div class="form-group">
                <input type="file" name="song" />
            </div>
            <div class="btn-group btn-group-block">
                <button class="btn btn-primary">Add Song</button>
            </div>
        </fieldset>
    </form>
    </div>
</main>